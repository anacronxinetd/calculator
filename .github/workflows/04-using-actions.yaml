name: 04 - Using Actions
on: 
  - push
  - workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        uses: actions/checkout@v4
      - name: Printing Folders
        run: ls
        
